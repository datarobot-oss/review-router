Dummy PR to verify the decoupled, reformatted Jira ticket link comment posts on PR open. Safe to close without merging. (Third attempt — first used the wrong label add flow, second used [-] which isn't a valid ticket ID.)
